node_modules/
.next/
*.tsbuildinfo
# per-phase agent scratch — disposable by construction (SPEC §7)
.ab/
# repository-local autobuild state (store, worktrees, and file tickets)
.autobuild/
*.local.db

# local secrets
.env

# macOS Finder metadata
.DS_Store
.vercel
.env*

# impeccable design skill: downloaded platform binary and transient round state
.agents/skills/impeccable/scripts/bin/
.claude/skills/impeccable/scripts/bin/
.impeccable/questions/
.impeccable/review/
.impeccable/build/
.impeccable/mocks/
